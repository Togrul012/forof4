const adlar = ['cabir','togrul','ilkin','elmaddin','miri','aysel','hurus']
for(const ad of adlar) {
    if(ad.length == 'a' && ad.length == 5) {
       console.log(`${ad} qatildir`);
       break
    }
    console.log(ad);
    
}